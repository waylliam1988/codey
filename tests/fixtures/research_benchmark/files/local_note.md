# Local analysis note (fixture)

The quarterly table in `local_table.csv` is the only revenue source for this
benchmark case. Q3 is the only quarter where churn rose while revenue fell.

Any conclusion drawn here must cite an analysis run that was actually executed
locally; reading this note alone is not a reproduction.
