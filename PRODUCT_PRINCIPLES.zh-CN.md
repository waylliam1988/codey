# Codey 产品原则

这份文档写给未来的自己和后来的维护者。

它不描述某个具体功能怎么写，而是说明 Codey 应该是什么、不应该变成什么，以及后续做功能时该怎么判断取舍。

## 一句话定位

Codey 是一个本地优先、低成本、多网页模型兼容的 AI 编程工作台。

它用用户已经能访问的网页版 AI 作为“大脑”，用本地程序提供网页 AI 缺少的能力：读写项目文件、运行测试、显示 diff、恢复改动、审批 shell、双模型 review。

Codey 的目标不是复制 Cursor、Claude Code、OpenClaw、Hermes 或 Pi Agent。它应该保持更小、更克制：

> 网页 AI 负责思考，本地 Codey 负责边界、工具、diff、恢复和验证。

## 为什么做 Codey

AI 编程不应该只属于买得起昂贵 API、订阅或大额度套餐的人。

很多人已经能使用网页 AI，但网页 AI 本身不能安全地读写本地项目、跑测试、展示 diff、撤销改动。Codey 要做的就是把这些能力接起来，让普通人可以用更低成本体验真实的 AI 编程闭环。

Codey 的价值不是“免费替代专业工具”，而是：

- 低门槛：先能用，再慢慢理解 Git、测试、diff。
- 低成本：优先使用用户自己的网页 AI 登录状态。
- 本地优先：代码和项目留在用户电脑上。
- 可见可控：每次改了什么都能看到。
- 可恢复：出错时尽量能 restore 或用 Git 回滚。
- 可验证：鼓励跑测试，而不是只相信模型文字。

## 目前 Codey 已经是什么

Codey 已经有一个可用的核心闭环：

1. 用户选择项目。
2. 用户用自然语言描述任务。
3. 网页 AI 通过 JSON 工具协议请求读文件、写文件、跑命令。
4. Codey 在本地执行受控工具。
5. Codey 显示红绿 diff。
6. 没有 Git 时也能 snapshot diff 和 restore。
7. 有 Git 时自动使用 Git diff。
8. shell 命令需要用户批准。
9. 任务失败时可以 retry。
10. 打开两个模型网页时，可以自动让第二个模型 review 代码改动。

这已经让 Codey 具备了“正经软件”的基本形态。

## 该做什么

### 继续加强核心闭环

优先级最高的永远是：

- 模型能稳定连接。
- 文件读写安全。
- diff 清楚。
- restore 可靠。
- shell 审批明确。
- 测试结果能反馈给模型。
- 网页 AI 改版后容易修对应 adapter。

任何新功能如果削弱这些基础能力，都应该暂停。

### 保持 UI 安静

Codey 的 UI 应该像一个安静的本地开发工具，而不是一个功能展示页。

用户不需要看到这些词：

- provider
- skill
- workflow
- agent graph
- planner
- memory
- tool registry

这些可以存在于底层，但不应该压到小白用户脸上。

用户应该只看到：

- 项目
- 聊天
- 模型选择
- 运行状态
- diff
- restore
- retry

### 用自然语言触发能力

用户不应该先选一个复杂模式再工作。

理想体验是：

```text
修测试
解释这个项目
做一个小游戏
把这个页面改好看一点
```

Codey 后台判断怎么执行。判断不准时，不要硬套流程，继续走普通 agent。

原则是：

> 宁可少识别，也不要乱识别。

### 少而强的工具

Codey 不应该堆一堆半成品工具。

当前最重要的工具很少：

- read
- write/edit
- run shell with approval
- diff
- restore
- review

很多看起来像新能力的东西，其实只是这些工具的组合，不需要做成新按钮。

### 双模型保持克制

双模型最有价值的地方是代码改动后的 review。

推荐模式：

```text
模型 A 写代码
模型 B 看 diff
模型 B 只指出具体问题
模型 A 根据 review 修一次
```

不建议做复杂群聊 UI。普通聊天阶段默认单模型即可。聊天里的第二模型补充意见很容易变成噪音，除非将来有很清楚的产品证据证明它有用。

## 不该做什么

### 不要做提示词 Skill

只靠提示词包装出来的 skill 不够可靠，也不优雅。

例如：

```text
你是 Python 专家，请按照以下步骤修复 bug...
```

这类内容可以作为 prompt 的一部分，但不应该被包装成产品能力。

真正有价值的能力应该是程序控制的流程或工具：

- Codey 负责跑测试。
- Codey 负责收集失败输出。
- Codey 负责限制上下文。
- Codey 负责落盘、diff、restore。
- 模型只负责需要智能判断的部分。

### 不要盲目添加 Actions

`Fix Tests`、`Explain Project`、`Create Small App` 这些能力 Codey 现在已经能通过通用 agent 完成。

未来如果要把它们固定成 action，不是为了新增能力，而是为了让高频任务更稳定、更短、更可测试。

在不知道真实高频使用场景之前，不要先做一套 action/skill 框架。

更好的路线是：

1. 继续使用通用 agent。
2. 观察哪些任务经常出现。
3. 观察哪些任务经常失败。
4. 只把真实高频、真实痛点固化成程序流程。

### 不要做插件市场

短期不做插件市场，不做大量第三方 skill。

原因：

- 安全边界会复杂。
- 小白用户难以理解。
- 维护成本会快速上升。
- 容易从“产品”变成“平台工程”。

Codey 现在更应该做少量内置、可测试、可维护的能力。

### 不要追求全能 Agent

OpenClaw、Hermes 这类全能 agent 的方向可以借鉴，但不要照搬。

Codey 不应该一开始就做：

- 长期后台自主乱跑
- 自动操作账号、邮箱、支付
- 大量插件
- 复杂记忆系统
- 多 agent 图形编排
- 自动执行 shell
- 不受限制地访问全电脑

这些会让系统很快变重，也会增加安全风险。

Codey 应该先把“本地项目编程闭环”做到稳定。

## 关于 Actions 的判断

未来也许可以有 actions，但它们必须满足这些条件：

1. 来自真实高频需求，而不是想象。
2. 能减少失败率或减少重复步骤。
3. 有明确安全边界。
4. 有测试。
5. 不增加主 UI 复杂度。
6. 不替代通用 agent，只是优化高频路径。

如果要做，优先考虑这三类：

### Fix Tests

固定流程：

1. 识别测试命令。
2. 请求用户批准运行测试。
3. 收集失败输出。
4. 让模型修复。
5. 再跑测试。
6. 修到通过或达到上限。

但在真实数据证明它高频之前，不要急着做。

### Explain Project

固定流程：

1. 只读项目。
2. 扫描目录结构。
3. 忽略缓存、大文件、构建目录。
4. 读取 README、配置、入口文件。
5. 给新手解释项目做什么、怎么运行、从哪里看起。

这个 action 风险较低，因为它默认不写文件。

### Create Small App

固定流程：

1. 判断项目是否为空，或用户是否明确要求创建。
2. 创建最小可运行结构。
3. 让模型补业务代码。
4. 做最低限度验证。
5. 展示 diff。

目标不是生成复杂工程，而是先生成一个能运行的小东西。

## 关于任务历史

Codey 现在已经有轻量历史雏形：

- 左侧有项目和聊天列表。
- 聊天消息保存在 localStorage。
- 每次任务会记录用户输入、模型回复、工具调用、shell 审批、错误、review、changes 摘要。
- 可以查看 diff。
- 可以 restore。

未来如果增强，不应该做复杂任务管理器。

更合理的是把一次运行整理成一张轻量“任务收据”：

```text
DONE · Fixed tests
Changes · 2 files
Tests · passed
Review · MiMo approved
```

这只是整理已有信息，不是新增一个重系统。

## 关于数据和统计

如果未来想知道哪些 action 值得做，可以先做本地统计，但必须克制。

可以记录任务形状：

```json
{
  "kind": "project_task",
  "provider": "deepseek",
  "result": "done",
  "changed_files": 2,
  "shell_used": true,
  "test_like_command": true,
  "stop_reason": "done"
}
```

不应该记录完整代码、完整 prompt、隐私文件内容。

这些统计只为判断产品方向，不应该变成监控用户。

## 设计取舍原则

每次想加功能时，先问：

1. 这个功能是不是用户已经反复遇到的真实问题？
2. 它能不能通过现有 agent 自然完成？
3. 如果能完成，为什么还要固定成流程？
4. 它会不会让 UI 变复杂？
5. 它会不会扩大安全边界？
6. 它有没有测试？
7. 它失败时用户能不能恢复？

如果答案不清楚，先不做。

## 最重要的一句话

Codey 应该是一个小而稳的本地 AI 编程工作台，不是一个为了展示架构而膨胀的 agent 平台。

它的长期生命力来自：

- 低成本
- 本地优先
- 多网页模型兼容
- diff 可见
- restore 可恢复
- 测试可验证
- UI 克制
- 失败后能修

保持这个方向，比堆更多概念更重要。
